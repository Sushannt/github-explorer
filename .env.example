# GitHub Personal Access Token (optional)
# Without this, the GitHub API allows 60 requests/hour per IP.
# With it, the limit increases to 5,000 requests/hour.
# Generate one at: https://github.com/settings/tokens (no scopes needed for public data)
GITHUB_TOKEN=
